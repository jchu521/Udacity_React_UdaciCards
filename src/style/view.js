import { StyleSheet } from 'react-native'

const view = StyleSheet.create({

  container: {
    flex: 1,
    margin: 20,
    justifyContent: 'center',
  },
  deckViewcontainer: {
    flex: 1,
    backgroundColor: '#fff',
    //alignItems: 'center',
    justifyContent: 'center',
  },
})

export default view
